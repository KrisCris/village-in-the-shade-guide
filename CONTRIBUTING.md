# 维护约定

- 以游戏数据为依据，保留内部 ID、构建号与核对记录。AppMedia 可以补充选题和线索，但不能代替游戏字段证据。
- 新增获取或用途关系时，同时检查物品、配方、机器等反向查询；原料机会成本不叫“买入价”，存在基础价格字段不等于商店实际出售。
- 收益计算区分加工增值和从种子开始的总收益，注明产量、季节、品质、重复收获和机器时间等假设。
- 新图标按游戏表引用提取，不按名称猜测或用相似图片冒充。提交的是站点使用的裁切图，不是完整纹理归档。
- 新页面的 `href`、`src` 及站内 `fetch` 使用 `src/lib/sitePath.ts` 的 `withBase`；不要把部署前缀写入游戏数据。
- 依赖变更同时更新 `package-lock.json`，日常安装使用 `npm ci`。Python 依赖使用 `uv.lock`。
- 只为改动相关的行为增加测试；合并 / 部署前运行 `npm run verify`。修改提取器时另运行 `uv run pytest`，需要真实游戏的检查在本地完成。
- 不提交 `.env`、原始游戏归档、存档、抓取缓存、截图日志和 `node_modules`。结构化快照与 `public/` 展示素材属于构建输入，必须保留。
- 临时研究输出放在被忽略的 `output/` 或 `.local/`，历史核对记录留在 `docs/`。
